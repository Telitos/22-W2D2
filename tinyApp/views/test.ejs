<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body background="blue.jpg">
<center>
<header>
  <% include ./partials/_header %>
</header>
<h1>Welcome to your personal URL repo</h1>
    <table style="width:100%, height:100%">
    <% Object.keys(urls).forEach(function(key) { %>
      <tr>
        <td><%= key %></td>
        <td>---></td>
        <td><a href="<%=`http://localhost:8080/u/${key}`%>"><%= urls[key]%></a></td>
        <td>
          <form method="POST" action="<%=`/urls/${key}/delete`%>">
            <input type="submit" value="Delete this URL">
          </form>
        </td>
        <td>
          <form method="GET" action="<%=`/urls/${key}`%>">
          <input type="submit" value="Edit this URL">
          </form>
        </td>
        </tr>
        <% }); %>
    </table>
    <table style="width:50%">
        <th>
          <a href="/urls/new">Click here to shorten your URL!</a>
        </th>
    </table>
  </center>
</table>
</body>
</html>
